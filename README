This is an experimental work. It works, but it's still experimental. I'll be updating
it and its documentation with time.

requirements:
 * nodejs
 * socket.io (to accept websocket clients)
 * mongoose (to save events and outgoing calls to mongo database)
 * log4js (for logging)

